import { IPXOptions } from 'ipx';
import { Handler } from '@netlify/functions';
import { RemotePattern } from './utils';
export interface IPXHandlerOptions extends Partial<IPXOptions> {
    cacheDir?: string;
    basePath?: string;
    propsEncoding?: 'base64' | undefined;
    bypassDomainCheck?: boolean;
    remotePatterns?: RemotePattern[];
    responseHeaders?: Record<string, string>;
}
export declare function createIPXHandler({ cacheDir, basePath, propsEncoding, bypassDomainCheck, remotePatterns, responseHeaders, ...opts }?: IPXHandlerOptions): Handler;
